## Security and Performance
Preserve the existing server boundaries and per-call agent resolution. Search only operates on data already visible to the user. Keep polling intervals and cached stale-data recovery. Verify dependencies using the installed lockfile and package audit.
